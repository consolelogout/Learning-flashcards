name: Déployer sur GitHub Pages

# Le job active lui-même GitHub Pages au premier lancement (enablement: true).
on:
  push:
    branches:
      - main
      - claude/flashcards-learning-control-modes-nm5qil
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Récupérer le dépôt
        uses: actions/checkout@v4

      - name: Configurer Pages
        uses: actions/configure-pages@v5
        with:
          # Active GitHub Pages (source « GitHub Actions ») si ce n'est pas déjà fait.
          enablement: true

      - name: Préparer l'artefact (le site est déjà statique)
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - name: Déployer
        id: deployment
        uses: actions/deploy-pages@v4
